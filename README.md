# stables

This package provides various helper functions for the analysis of aerial survey data, particularly to prepare data for analysis using the `inlabru` and `MRSea` packages.

The package is in development, and more functions will be added as they are developed by DMP. 

To install the package, use `devtools`:

```
devtools::install_github("dmpstats/stables")
```
