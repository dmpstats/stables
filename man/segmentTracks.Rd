% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentTracks.R
\name{segmentTracks}
\alias{segmentTracks}
\title{segmentTracks}
\usage{
segmentTracks(
  trackdata,
  seg_length,
  obsdata = NULL,
  output_type = c("points", "lines"),
  camera_width = 125,
  transect_width = NULL,
  polygonize = FALSE
)
}
\arguments{
\item{trackdata}{An sf dataframe of survey tracks (LINESTRING or MULTILINESTRING).}

\item{seg_length}{Numeric. Intended length of each segment in metres.}

\item{obsdata}{Optional. An sf dataframe of observation points, must contain a 'Camera' column. This should be provided if a different number of cameras were reviewed on each survey, as transect widths will differ. This should be provided BEFORE filtering to survey-level, i.e. not the output of prepObsPPM.}

\item{output_type}{Character: either "points" (default) to return segment centroids as points, or "lines" to return segment linestrings.}

\item{camera_width}{Numeric: width of each camera's field of view in metres. Only used if obsdata is not provided. Default is 125 metres.}

\item{transect_width}{Numeric: width of each transect in metres. Only used if obsdata is not provided. If both obsdata and transect_width are provided, obsdata takes precedence.}

\item{polygonize}{Logical: if TRUE, segments will be converted to polygons representing the transect area. Default is FALSE.}
}
\value{
An sf dataframe of segmented tracks, optionally with associated observations.
}
\description{
Splits survey tracks into regular segments and optionally associates observations with segments.
}
\details{
- Validates input types and required columns.
- Segments tracks into regular lengths.
- Associates observations with segments if provided.
}
\examples{
\dontrun{
segments <- segmentTracks(trackdata, 1000)
segments_with_obs <- segmentTracks(trackdata, 1000, obsdata)
}
}
