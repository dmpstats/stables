% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/split_lines.R
\name{split_lines}
\alias{split_lines}
\title{Split Linestrings into Substrings of Maximum Length}
\usage{
split_lines(input_lines, max_length, id = "ID")
}
\arguments{
\item{input_lines}{An sf object containing linestring geometries to be split.}

\item{max_length}{Numeric. The maximum allowed length for each segment. Units should match the CRS of `input_lines`.}

\item{id}{Character. Name of the identifier column for the input features. Default is "ID".}
}
\value{
An sf object with the split linestrings as geometry. All original columns are retained, with additional columns:
  \describe{
    \item{fID}{Original feature ID.}
    \item{piece}{Piece number within the original feature.}
    \item{start}{Normalized start position (0-1) of the substring.}
    \item{end}{Normalized end position (0-1) of the substring.}
  }
}
\description{
Splits linestring geometries in an sf object into smaller segments, each no longer than a specified maximum length.
This is useful for processing or visualizing long linestrings in manageable pieces.
}
\details{
Linestrings shorter than `max_length` are not split. A warning is issued if `max_length` is suspiciously small.
Requires the `lwgeom` and `progress` packages.
}
\examples{
\dontrun{
library(sf)
lines <- st_read("lines.shp")
split <- split_lines(lines, max_length = 1000)
}

}
